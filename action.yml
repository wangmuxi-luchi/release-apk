name: "Release APK"
branding:
  icon: "package"
  color: "red"
description: "Release APK to your GitHub repo"
author: "jebarpg"
inputs:
  GITHUB_TOKEN:
    description: "Github secret token to allow automated publishing of APK to release."
runs:
  using: "docker"
  image: "Dockerfile"
